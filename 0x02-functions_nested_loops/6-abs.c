#include "main.h"

/**
 * _abs - Gives the absolute value of integer
 * @i: The integer to be computed
 * Return: The absolute value of i
 */

int _abs(int i)
{
	if (i >= 0)
		return (i);
	else
		return (-i);
}
